## Core (required to run the app)
streamlit>=1.35
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
plotly>=5.20
joblib>=1.3
requests>=2.28

## Optional, strongly recommended - enables two LIVE OpenStreetMap-based
## features: real diversion routing in geo_utils.get_diversion_suggestion(),
## and road-aware barricade/officer placement in compute_deployment_points()
## (snaps to the nearest real junction and follows its actual approach
## roads, instead of an arbitrary evenly-spaced ring). Both free, no API key
## - OSM is open data. Without this installed, both features automatically
## fall back to dataset-driven approximations, so the app still runs fully
## either way - this just makes the live demo noticeably more convincing.
## Uncomment to enable:
# osmnx>=1.9
# networkx>=3.0
